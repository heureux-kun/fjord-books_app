<h1><%= t'title.list', model: t('activerecord.models.user') %></h1>
<ul>
<% @users.each do |user| %>
  <li>
    <%= link_to user_path(user) do %>
      <%= user.id %>：<%= user.email %>
    <% end %>
  </li>
<% end %>
</ul>
<%= paginate @users %>