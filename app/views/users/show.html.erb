<h1><%= t('views.common.title_show', name: User.model_name.human) %></h1>

<p>
  <strong><%= User.human_attribute_name(:email) %>:</strong>
  <%= @user.email %>
</p>

<p>
  <strong><%= User.human_attribute_name(:name) %>:</strong>
  <%= @user.name %>
</p>

<p>
  <strong><%= User.human_attribute_name(:postal_code) %>:</strong>
  <%= @user.postal_code %>
</p>

<p>
  <strong><%= User.human_attribute_name(:address) %>:</strong>
  <%= @user.address %>
</p>

<p>
  <strong><%= User.human_attribute_name(:self_introduction) %>:</strong>
  <%= @user.self_introduction %>
</p>

<p><%= Relationship.human_attribute_name(:following) %>：<%= link_to @user.followings.count, user_followings_path(@user) %></p>
<p><%= Relationship.human_attribute_name(:follower) %>：<%= link_to @user.followers.count, user_followers_path(@user) %></p>

<% unless current_user == @user %>
  <% if current_user.following?(@user) %>
    <%= form_with model: current_user.relationships.find_by(follow_id: @user.id), html: { method: :delete } do |f| %>
      <%= hidden_field_tag :follow_id, @user.id %>
      <%= f.submit Relationship.human_attribute_name(:unfollow) %>
    <% end %>
  <% else %>
    <%# form_with url: relationships_path do ~ でもいける %>
    <%= form_with model: current_user.relationships.build do |f| %>
      <%= hidden_field_tag :follow_id, @user.id %>
      <%= f.submit Relationship.human_attribute_name(:follow) %>
    <% end %>
  <% end %>
<% end %>

<p>
  <strong><%= User.human_attribute_name(:avatar) %>:</strong>
  <% if @user.avatar.attached? %>
    <br>
    <%= image_tag @user.avatar.variant(resize_to_fill: [150, 150])%>
  <% end %>
</p>

<% if current_user == @user %>
  <%= link_to t('views.common.edit'), edit_user_registration_path %> |
<% end %>
<%= link_to t('views.common.back'), users_path %>
